import express from 'express';

console.log (express);

const express = require('express;')

const server = express ();

localhost:80000/pedido
server.get('/pedido', () => 
{ console.log("Acessou a Rota")
});
server.listen(8000);

